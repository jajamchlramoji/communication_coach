# Recall Configuration
# Server Port
PORT=3001

# Gemini API Key (keep this private; never commit this file or expose in frontend code)
GEMINI_API_KEY=your_gemini_api_key_here

# Mode: set to "true" to force local simulation demo mode without contacting external APIs
DEMO_MODE=false
